<nav class="navBarOne navbar navbar-expand-md navbar-dark bg-navBg" style="max-height: 60px; min-width: 790px, box-shadow: 5px, 5px, 5px">
  <!-- logo -->
  <a class="navbar-brand" href="/">Habi-tat</a>
  <button type="button" class="navbar-toggler" data-toggle="collapse" data-target="#navbarCollapse">
    <span class="navbar-toggler-icon"></span>
  </button>

  <!-- Habits Page -->
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item active">
        <a class="nav-link" href="/">Home <span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/habits">Habits</a>
      </li>
    </ul>
  </div>

  <!-- search bar -->
    <form class="form-inline col-9 d-flex justify-content-center ml-auto" action="/search">
      <input type="text" class="form-control mr-sm-2 col-lg-5" placeholder="Search" name="q">
      <button type="submit" class="searchBtn btn btn-outline-light mr-1"><%= image_tag("logos/whitesearch.png", width: '20px') %></button>
    </form>
  <!-- right nav container -->
    <div class="userNav d-flex justify-content-end col-md-2" style="margin-left: -110px">
      <% if user_signed_in? %>
      <p class="nav navbar-text navbar-right" style="font-weight: bold">Hello,</p>
      <ul class="nav navbar-nav">
        <li class="nav-item">
        <%= link_to current_user.username, destroy_user_session_url, method: :delete, class: 'nav-link' %>
        </li>
      </ul>
      <% else %>
      <a href="/users/sign_in" class="nav-item nav-link signNav">Sign In</a>
      <% end %>
    </div>
  </div>
</nav>